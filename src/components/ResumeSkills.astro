---
interface Props {
  skills: string[];
}

const { skills } = Astro.props;
---

<div id="skills-list" class="flex flex-wrap gap-2 mt-2">
  {
    skills.map((skill) => {
      return (
        <span class="bg-gray-100! px-3 py-1 rounded-md text-sm">{skill}</span>
      );
    })
  }
</div>
